{
  "name": "web-layout-training-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --no-open --port 80",
    "index": "start chrome.exe --new-window http://localhost/A.py/pages/index.html && npm run dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist -r gitlab",
    "push": "git push gitlab main"
  },
  "devDependencies": {
    "ejs": "^3.1.9",
    "vite": "^4.2.0",
    "vite-plugin-ejs": "^1.6.4"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "glob": "^10.2.2",
    "sass": "^1.61.0",
    "sass-loader": "^13.2.2",
    "vite-plugin-live-reload": "^3.0.2"
  }
}

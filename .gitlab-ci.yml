stages:
  - build
  - deploy

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist

deploy:
  stage: deploy
  script:
    - mkdir .public
    - cp -r dist/* .public
    - mv .public pages
  artifacts:
    paths:
      - pages
  only:
    - main
